# TODO

Open items only. Completed work is dropped from here — the CHANGELOG
is the record of what shipped.

- [ ] Wire up the optional edge hook `lua/dark_canary.lua` (needs nginx-lua-waf-kit).
- [ ] Run it against a real service end to end — everything else exists to make that possible.
